# Session History — 2026-05-01

> **범위**: spec_03 PR #3 리뷰 대응, GitHub Pages 수정, verify_local.sh 개편
> **결과**: PR #3 머지 완료, Pages 빌드 복구, CI 스크립트 강화

---

## 1. PR #3 리뷰 대응 및 머지

### 1차 리뷰 (Claude/Gemini) — CHANGES REQUESTED

| 등급 | 이슈 | 수정 |
|------|------|------|
| 🔴 Blocker | `jobId` 유실 — 모든 공고 ID가 `""` | `JobRepository`에서 `doc.id`를 `JobModel`에 주입 |
| 🟠 Major | `locationCode`+`jobCategory` 동시 필터 인덱스 없음 | `firestore.indexes.json`에 4번째 인덱스 추가 |
| Minor | Mock 회사명 가독성 (`언더스코어`) | `아파트 경비원 1호 관리사무소` 형식으로 개선 |
| Minor | `seed_jobs.py` 비재현성 | `random.seed(42)` 고정 |
| — | 회귀 방지 | `test/models/job_model_test.dart` 3건 추가 |

**CI**: `flutter analyze` 0 issues, `flutter test` 11/11 passed

### 2차 리뷰 (Claude/Gemini) — APPROVED ✅

PR #3 → master 머지 완료. spec_03 `✅ 완료` 처리.

커밋: `faf7348` (수정), `66f5ed0` (graphify), `75ccf12` (merge)

---

## 2. GitHub Pages 빌드 실패 수정

### 원인

1. **경로 불일치**: 모노레포 통합 후 `docs/` 이하로 이동했으나 `pages.yml`이 옛날 경로 참조
2. **`wiki.html` 누락**: 인덱스 페이지 파일이 존재하지 않음
3. **GitHub Pages 미활성화**: 리포지토리 Settings에서 Pages가 꺼져있었음

### 수정

- `pages.yml`: 모든 `cp` 경로에 `docs/` 접두사 추가
- `docs/wiki.html`: 스펙 현황 + 문서 링크 포함한 인덱스 페이지 생성
- `gh api repos/.../pages --method POST`: `build_type=workflow`로 Pages 활성화

### 결과

- Pages URL: https://littley-y.github.io/SilverWorker/
- Workflow: ✅ success (연속 2회)

커밋: `16230c0` (수정), `ecbfde0` (graphify)

---

## 3. `verify_local.sh` 전면 개편

### 개선 사항

| 항목 | 이전 | 이후 |
|------|------|------|
| Pages 검증 | 파일 존재 확인 | 실제 `cp` dry-run 시뮬레이션 (pages.yml 그대로 실행) |
| Graphify | 없음 | 미커밋 변경 감지 + 경고 |
| Android 빌드 | 없음 | `--ci` 플래그로 `flutter build apk --debug` 추가 |
| 단계 표시 | 정적 `[1/5]` | 동적 `[N/TOTAL]` (6 기본 / 7 CI) |

### 사용법

```bash
tools/verify_local.sh        # 기본 (6단계, ~8초)
tools/verify_local.sh --ci   # CI 모드 (7단계, +Android 빌드)
```

커밋: `71d4080`

---

## 4. 현재 상태

| Spec | Day | 상태 |
|------|-----|------|
| spec_01 | 1 | ✅ 완료 |
| spec_02 | 2 | ✅ 완료 |
| spec_03 | 3 | ✅ 완료 (PR #3 머지) |
| spec_04 | 4 | ⬜ 대기 |

**Master 브랜치**: 모든 완료 스펙 머지 완료. Day 4 (spec_04 공고 목록 UI) 대기 중.
