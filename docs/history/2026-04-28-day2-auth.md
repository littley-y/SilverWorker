# Day 2 Implementation — Firebase Phone Auth & Profile Setup

**Date**: 2026-04-28
**Session**: Day 2 (spec_02) — 인증 및 프로필 등록
**Status**: 🔄 Awaiting Review
**Branch**: `feature/day2-auth`
**PR**: #2

---

## 1. Goal

Implement the complete Firebase Phone Auth flow and profile registration screen as specified in `docs/planning/spec_02_auth.md`.

---

## 2. Changes Summary

### New Files
| Path | Description |
|---|---|
| `lib/router/app_router.dart` | go_router configuration with 4 routes (`/auth/phone`, `/auth/otp`, `/auth/profile`, `/`) and auth-based redirect logic |
| `lib/screens/auth/phone_input_screen.dart` | Phone number input with `+82` prefix, Firebase `verifyPhoneNumber` integration |
| `lib/screens/auth/otp_input_screen.dart` | 6-digit SMS code input with 60-second countdown and resend |
| `lib/screens/main/main_screen.dart` | Post-auth landing screen with profile existence check |
| `lib/constants/address_data.dart` | Korean administrative districts (sido/sigungu) for address selection |
| `test/constants/address_data_test.dart` | Unit tests for address data |
| `test/models/user_model_test.dart` | Unit tests for UserModel serialization |

### Modified Files
| Path | Change |
|---|---|
| `lib/main.dart` | Switched from `MaterialApp` to `MaterialApp.router` using `routerProvider` |
| `lib/repositories/auth_repository.dart` | Added `verifyPhoneNumber`, `signInWithCredential`, `createProfile` methods |
| `lib/providers/auth_provider.dart` | Added `verificationIdProvider`, `phoneNumberProvider`, `authLoadingProvider` + helper functions `startPhoneVerification`, `verifyOtp` |
| `lib/screens/auth/profile_register_screen.dart` | Complete rewrite: name input, sido/sigungu dropdowns, career textarea (500 char limit), Firestore save |
| `pubspec.yaml` | Added `go_router: ^14.0.0` dependency |

### Deleted Files
| Path | Reason |
|---|---|
| `lib/screens/auth/login_screen.dart` | Replaced by `phone_input_screen.dart` |

---

## 3. Auth Flow

```
App Launch
  ↓
GoRouter checks FirebaseAuth.currentUser
  ├─ Not logged in → /auth/phone
  │    ↓
  │  PhoneInputScreen → verifyPhoneNumber()
  │    ↓
  │  /auth/otp ← verificationId
  │    ↓
  │  OtpInputScreen → signInWithCredential()
  │    ↓
  │  / ← auto-redirect on auth state change
  │    ↓
  │  MainScreen checks Firestore profile
  │      ├─ No profile → /auth/profile
  │      └─ Has profile → JobListScreen
  │
  └─ Logged in + has profile → / (MainScreen → JobListScreen)
```

---

## 4. UI Specifications

### PhoneInputScreen
- Title: "휴전폰 번호로 시작하세요" (24pt Bold)
- Input: `+82` prefix (fixed) + 10–11 digit number
- Button: "인증번호 받기" (56dp height, primary color)
- Error: red border + "올바른 번호를 입력하세요"

### OtpInputScreen
- Title: "인증번호 6자리를 입력하세요" (22pt Bold)
- 6 separate input fields (56dp × 64dp each)
- Resend button: 60-second countdown, then enabled
- Verify button: disabled until all 6 digits entered

### ProfileSetupScreen
- Name: required, max 20 chars
- Address: 2-step dropdown (sido → sigungu)
- Career summary: optional, max 500 chars with live counter
- Submit: "시작하기" (disabled until name + address selected)

---

## 5. Firestore Schema

`createProfile` writes to `/users/{uid}`:

```dart
{
  'userId': uid,
  'phoneNumber': phoneNumber,
  'name': name,
  'address': {'sido': sido, 'sigungu': sigungu},
  'careerSummary': careerSummary,
  'gender': '',
  'physicalConditions': [],
  'preferredJobTypes': [],
  'preferredLocations': [],
  'isPushEnabled': true,
  'createdAt': FieldValue.serverTimestamp(),
  'updatedAt': FieldValue.serverTimestamp(),
}
```

Aligned with `docs/planning/overview/04_db_schema.md` §2.1.

---

## 6. Validation Results

| Check | Result |
|---|---|
| `flutter analyze` | ✅ No issues |
| `dart format` | ✅ All files formatted |
| `flutter test` | ✅ 8 tests passed |
| `tools/verify_local.sh` | ✅ All 4 steps passed |

---

## 7. Known Limitations / Day 3+ Follow-ups

- `smsRetriever` / Android auto-fill API not yet integrated (Day 2 nice-to-have)
- Guest login (AUTH-04, P1) not implemented — deferred to 2nd iteration
- `MainScreen` is a placeholder delegating to `JobListScreen`; Day 8 will add BottomNavigationBar
- Physical conditions checklist (spec_03/ui_system) not included in profile

---

## 8. References

- Spec: `docs/planning/spec_02_auth.md`
- DB Schema: `docs/planning/overview/04_db_schema.md`
- MVP Specs: `docs/planning/overview/03_mvp_specs.md`
