# PR #7 Review Request — spec_07 마이페이지 (Day 9)

**날짜**: 2026-05-06
**브랜치**: `feature/day9-mypage`
**대상 스펙**: [spec_07_mypage.md](../../planning/spec_07_mypage.md)
**구현자**: Sisyphus (OpenCode)

---

## 변경 요약

마이페이지 화면과 지원 내역 목록 화면을 구현합니다.

| 파일 | 변경 |
|---|---|
| `lib/screens/mypage/my_page_screen.dart` | 프로필 요약 카드, 메뉴 리스트, 로그아웃 버튼/다이얼로그 구현 |
| `lib/screens/mypage/application_list_screen.dart` | 지원 내역 목록 + ApplicationCard 위젯 신규 |
| `lib/router/app_router.dart` | `/mypage`, `/mypage/applications` 라우트 추가 |
| `test/widgets/my_page_screen_test.dart` | MypageScreen 위젯 테스트 6건 신규 |
| `test/widgets/application_list_screen_test.dart` | ApplicationListScreen 위젯 테스트 5건 신규 |
| `docs/PROGRESS.md` | spec_07 상태 갱신 |
| `docs/history/2026-05-06-day9-mypage.md` | 구현 이력 기록 |

---

## 스펙 대비 구현 체크리스트

### MypageScreen
- [x] 프로필 요약 카드: 이름(22pt Bold), 거주 지역(18pt), 경력 소개(16pt, max 2줄 + ellipsis)
- [x] 지원 횟수 뱃지: "총 N건 지원"
- [x] 메뉴 리스트: "지원 내역" → ApplicationListScreen (찜한 공고, 알림 설정은 placeholder)
- [x] 로그아웃 버튼: 하단 텍스트 버튼, 빨간색
- [x] 로그아웃 확인 다이얼로그: "로그아웃 하시겠습니까?" / "취소" / "로그아웃"
- [x] 로그아웃 확인 시 `FirebaseAuth.instance.signOut()` 호출 → authStateProvider가 null 방출 → go_router redirect로 로그인 화면 이동
- [x] `userProfileProvider`, `myApplicationsProvider` 사용
- [x] loading / error / null profile 상태 모두 처리

### ApplicationListScreen
- [x] AppBar: "지원 내역"
- [x] ListView: ApplicationCard × N
- [x] EmptyState: "아직 지원한 공고가 없습니다"
- [x] `submittedAt` 내림차순 (repository 레벨 `orderBy` 처리)

### ApplicationCard
- [x] 공고 제목: 18pt Bold
- [x] 회사명: 16pt 회색
- [x] 지원 일시: 14pt 회색, "MM월 DD일" 형식
- [x] 상태 배지 우측, 색상 구분:
  - [x] `submitted` → 파랑 (접수)
  - [x] `reviewing` → 주황 (검토 중)
  - [x] `accepted` → 초록 (합격)
  - [x] `rejected` → 빨강 (불합격)
  - [x] `cancelled` → 회색 (취소됨)

---

## 테스트

| 테스트 파일 | 케이스 수 | 주요 검증 |
|---|---|---|
| `test/widgets/my_page_screen_test.dart` | 6 | 프로필 렌더링, 지원 횟수 뱃지, 메뉴/로그아웃 버튼, 다이얼로그, 로딩 상태 |
| `test/widgets/application_list_screen_test.dart` | 5 | 빈 상태, 카드 렌더링, 5종 상태 배지 색상, 에러/로딩 상태 |

**전체 테스트**: `flutter test` 62/62 통과

---

## 검증

```bash
$ bash tools/verify_local.sh
✅ Dependencies resolved.
✅ Formatting is clean.
✅ Zero warnings. (flutter analyze)
✅ All tests passed. (62/62)
✅ Pages build simulation passed.
```

---

## 리뷰 포인트

1. **MypageScreen UI**: 프로필 카드와 메뉴 리스트의 시니어 친화도 (폰트 크기, 대비, 터치 영역)
2. **ApplicationCard 상태 배지**: 색상이 `AppColors` 상수와 정확히 매핑되는지
3. **로그아웃 흐름**: `FirebaseAuth.instance.signOut()`만 호출하고 go_router redirect에 의존하는 방식이 적절한지
4. **라우터**: `/mypage`와 `/mypage/applications` 경로가 go_router 스타일 가이드에 맞는지
5. **테스트 커버리지**: Edge case (empty, loading, error)가 충분히 커버되는지

---

## 리뷰어

- [ ] Claude Code
- [ ] Gemini CLI

---

*Review 후 수정 사항은 본 브랜치에 추가 커밋으로 반영 예정*
