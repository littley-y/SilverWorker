# PR #10 Review Request — spec_10 (Day 12) 테스트 기준 및 DoD

> Date: 2026-05-10
> Branch: master (direct commit 후 리뷰 요청)
> Spec: [spec_10_test_criteria.md](../planning/spec_10_test_criteria.md)

---

## 변경 요약

Day 12 스펙인 **테스트 기준 및 Definition of Done**을 구현했습니다. 기존 12개 테스트 파일(60테스트)에 3개 신규 파일을 추가하여 **총 15개 파일, 84개 테스트**를 완성했습니다.

---

## 주요 변경사항

### 1. 신규 테스트 파일 3개

| 파일 | 테스트 수 | 커버리지 |
|---|---|---|
| `test/repositories/application_repository_test.dart` | 7 | 중복 지원 방지, 예외 처리 |
| `test/models/job_filter_test.dart` | 6 | 필터 조합, copyWith 동작 |
| `test/models/physical_badge_test.dart` | 8 | 강도 레이블, 배지 enum 변환 |

### 2. 리팩토링

- `ApplicationRepository`: `FirebaseAuth`를 생성자 주입으로 변경 (`auth` 파라미터 추가) → 단위 테스트에서 mock 주입 가능

### 3. Android 버그 수정 (추가)

- `android/app/src/main/kotlin/com/silverworkernow/app/MainActivity.kt` 신규 생성 (누락으로 인한 `ClassNotFoundException`)
- `AndroidManifest.xml`에 `INTERNET` 권한 추가 (release APK에서 Firebase 연결 불가)
- `google-services.json` 업데이트 (SHA-1 지문 반영)

---

## 품질 검증 결과

```bash
$ flutter analyze
No issues found!

$ flutter test
84 tests passed
```

---

## 리뷰 포인트

### Claude
- [ ] `ApplicationRepository` 생성자 주입 패턴이 일관성 있는지
- [ ] `fake_cloud_firestore`를 사용한 트랜잭션 테스트가 충분한지
- [ ] `JobFilter.copyWith` sentinel 패턴 테스트가 null 설정을 정확히 검증하는지
- [ ] `physicalBadge_test.dart`의 const 생성자 호출이 번거롭지 않은지 (fixture 활용 여부)

### Gemini
- [ ] 테스트 커버리지가 spec_10의 4개 파일 요구사항을 충족하는지
- [ ] 예외 케이스(AlreadyAppliedException, JobClosedException 등)가 누락 없이 테스트되는지
- [ ] AndroidManifest.xml의 권한 추가가 최소한인지
- [ ] 불필요한 import가 없는지 (`flutter analyze`는 통과)

---

## 체크리스트

- [x] `flutter analyze` 0경고
- [x] `flutter test` 84/84 통과
- [x] `docs/PROGRESS.md` 업데이트 완료
- [x] Review Request 문서 작성 완료
- [x] Forbidden 파일 미포함 (AGENTS.md, PROMPT 파일 등)
