# PR #4 Review Request — Day 4: 공고 목록 UI

- **브랜치**: `feature/day4-job-list-ui` → `master`
- **구현자**: OpenCode (Sisyphus)
- **날짜**: 2026-05-05
- **대상 Spec**: [spec_04_job_list_ui.md](../planning/spec_04_job_list_ui.md), [spec_09_ui_system.md](../planning/spec_09_ui_system.md) (기반)
- **CI 결과**: `tools/verify_local.sh` 6/6 통과 (0 errors, 0 warnings, 26/26 tests passed)

---

## 변경 범위

### 신규 파일 (3)
| 파일 | 설명 |
|---|---|
| `lib/widgets/filter_bar.dart` | 지역 칩(4종) + 직종 칩(6종) 수평 스크롤 필터바. 단일 선택/토글. |
| `test/widgets/job_card_test.dart` | JobCard 렌더링 8종 테스트 (제목, 급여, 고용칩, D-n, 상시/마감, 탭) |
| `test/widgets/filter_bar_test.dart` | FilterBar 동작 7종 테스트 (렌더링, 선택/해제, 색상) |

### 수정 파일 (11)
| 파일 | 변경 내용 |
|---|---|
| `lib/constants/app_text_styles.dart` | spec_09 정렬: caption 16→14pt, button 18→20pt, sectionTitle 16pt 추가 |
| `lib/constants/app_colors.dart` | spec_09 정렬: textSecondary/background 수정, divider/cardBackground/intensity/status 신규 |
| `lib/screens/auth/*.dart` (3) | `AppColors.surface` → `AppColors.background` 마이그레이션 |
| `lib/widgets/job_card.dart` | 제목/회사/급여/고용형태칩/D-n마감일/강도배지 전체 구현 |
| `lib/screens/job/job_list_screen.dart` | AppBar("은빛일자리"), FilterBar, ListView, 로딩/에러/빈 상태 구현 |
| `lib/router/app_router.dart` | `/job/:jobId` 라우트 추가 |
| `lib/screens/job/job_detail_screen.dart` | jobId 파라미터 수용 (Day 5 placeholder) |
| `lib/main.dart` | `MaterialApp.router()` + `routerProvider` 버그 수정 |
| `docs/PROGRESS.md` | Day 4 / spec_09 진행 중으로 업데이트 |

---

## Spec DoD 충족 여부

| DoD 항목 | 상태 | 비고 |
|---|---|---|
| 공고 목록이 카드 형태로 표시됨 | ✅ | JobCard: 제목(20pt), 회사(16pt), 급여(18pt bold), 고용칩, D-n, 강도배지 |
| 지역/직종 필터 칩 탭 시 목록 변경 확인 | ✅ | FilterBar → jobFilterProvider → jobListProvider 자동 재조회 |
| 18pt 이상 폰트 전체 적용 확인 | ✅ | body 18pt, title 20pt, headline 24pt, button 20pt (caption은 14pt로 spec_09 기준) |
| 에뮬레이터에서 스크롤 동작 확인 | ⚠️ | 위젯 테스트로 구조 검증 완료, 에뮬레이터 실기기 테스트 필요 |

---

## 추가 작업 (spec 범위 외)
- `main.dart`가 `GoRouter`를 사용하지 않던 버그 수정 (라우팅 전체 미작동)
- spec_09 UI 시스템 기반 정렬 (AppTextStyles, AppColors)
- `JobCard.copyWith` nullable 필드 null 설정 불가 이슈 발견 → FilterBar에서 직접 `JobFilter` 생성으로 우회 (copyWith 자체는 수정 보류)

---

## 리뷰어 체크리스트

- [ ] `flutter analyze` 0 issues 확인
- [ ] `flutter test` 26/26 통과 확인
- [ ] JobCard 레이아웃: 16dp 패딩, 12dp 라운드, elevation 2, 최소 88dp 높이
- [ ] FilterBar 칩: 선택 시 파란 배경, 미선택 시 흰 배경+테두리, 재탭 해제
- [ ] 로딩/에러/빈 상태 각각 정상 표시
- [ ] `main.dart` → `MaterialApp.router()` + `routerProvider` 정상 연결
- [ ] `AppColors` / `AppTextStyles` spec_09 §1~2 와 일치 여부
