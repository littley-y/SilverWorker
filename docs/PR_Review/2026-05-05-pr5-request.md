# PR #5 Review Request — Day 5: 공고 상세 + 세이프티 큐레이션

- **브랜치**: `feature/day5-job-detail` → `master`
- **구현자**: OpenCode (Sisyphus)
- **날짜**: 2026-05-05
- **대상 Spec**: [spec_05_job_detail.md](../planning/spec_05_job_detail.md)
- **CI 결과**: `flutter analyze` 0 issues, `flutter test` 42/42 passed

---

## 변경 범위

### 신규 파일 (3)
| 파일 | 설명 |
|---|---|
| `lib/widgets/safety_curation_section.dart` | 업무 강도 등급 박스(3단계 컬러) + 신체 부담 배지(6종 Wrap) |
| `test/widgets/safety_curation_section_test.dart` | SafetyCurationSection 4종 테스트 |
| `test/widgets/job_detail_screen_test.dart` | JobDetailScreen 5종 테스트 |

### 수정 파일 (2)
| 파일 | 변경 내용 |
|---|---|
| `lib/screens/job/job_detail_screen.dart` | placeholder → 전체 구현 (헤더/강도/근무조건/자격요건/복리후생/업무내용 + 하단 고정 지원하기 버튼) |
| `lib/providers/job_provider.dart` | `jobDetailProvider` (FutureProvider.family) 추가 |

---

## Spec DoD 충족 여부

| DoD 항목 | 상태 | 비고 |
|---|---|---|
| 공고 목록에서 카드 탭 → 상세 화면 진입 확인 | ✅ | Day 4 `context.push('/job/${job.jobId}')` 연동 |
| physicalIntensity 등급이 색상 + 텍스트로 표시됨 | ✅ | light(녹색)/moderate(주황)/heavy(빨강) + 10% 투명 배경 |
| physicalBadges 배지가 1개 이상 표시됨 | ✅ | 6종(standing/sitting/heavy_lifting/outdoor/repetitive/stairs) |
| 하단 "지원하기" 버튼이 스크롤해도 고정되어 있음 | ✅ | `Stack` + `Positioned(bottom:0)` + `SafeArea` |
| 뒤로가기 버튼으로 목록 복귀 확인 | ✅ | AppBar 기본 뒤로가기 |

---

## 구현 상세

### SafetyCurationSection (`lib/widgets/safety_curation_section.dart`)
- `_IntensityGradeBox`: 48dp 높이, `_color.withValues(alpha: 0.1)` 배경, 아이콘 + 텍스트
- `_PhysicalBadgeChip`: 36dp 높이, 연회색 배경, 아이콘 + 텍스트, `Wrap` 레이아웃
- 빈 배지 배열 시 "신체 부담 항목" 섹션 숨김 처리

### JobDetailScreen (`lib/screens/job/job_detail_screen.dart`)
- `ConsumerWidget` + `jobDetailProvider(jobId)` 로 데이터 fetch
- `_HeaderSection`: 제목(24pt headline), 회사명, 급여(22pt bold primary)
- `SafetyCurationSection`: 강도 + 배지
- `_WorkConditionSection`: 근무지/급여/시간/요일/기간/고용형태 6행 테이블
- `_SectionBlock`: 자격요건/복리후생/업무내용 (빈 값은 "정보 없음" 표시)
- 하단 버튼: `Stack`+`Positioned`+`SafeArea`, 56dp 높이, primary 색상
- `ScrollView` 하단 패딩 96dp → 버튼 영역 확보

---

## 리뷰어 체크리스트

- [ ] `flutter analyze` 0 issues 확인
- [ ] `flutter test` 42/42 통과 확인
- [ ] SafetyCurationSection: 3단계 강도(light/moderate/heavy) 색상·아이콘·텍스트 정확
- [ ] SafetyCurationSection: 6종 배지 라벨 한글 매핑 정확
- [ ] SafetyCurationSection: 빈 배지 시 섹션 숨김
- [ ] JobDetailScreen: 급여 표시 시급/일급/월급 분기 정확 (spec_04 B-1과 동일 패턴)
- [ ] JobDetailScreen: 근무조건 테이블 고용형태 한글 변환
- [ ] JobDetailScreen: 하단 버튼 SafeArea 적용, 노치 영역 침범 없음
- [ ] JobDetailScreen: null job → "공고를 찾을 수 없습니다" 표시
- [ ] `jobDetailProvider` → `JobRepository.fetchJobById()` 호출 경로 정상
- [ ] 14pt 미만 텍스트 없음 (spec_09 폰트 하한)
